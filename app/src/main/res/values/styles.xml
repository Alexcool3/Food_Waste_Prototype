<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/Orange</item>
        <item name="colorPrimaryDark">@color/Orange</item>
        <item name="colorAccent">@color/Orange</item>
    </style>


    <style name="FoodWasteTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorAccent">@color/Orange</item>
        <item name="colorPrimary">@color/Beige</item>
        <item name="color">@color/Yellow</item>
        <!--   <item name="color">@color/Green</item>
           <item name="color">@color/Red</item>
           <item name="color">@color/Black</item>
           <item name="color">@color/White</item>-->
           <item name="android:fontFamily">@font/ibmplex</item>
           <item name="title">@font/ibmplexsansregular</item>
       </style>
   </resources>
